[alias]      porcelain = status --porcelain
             addall = add -uv --renormalize
             send = "!git pull --verbose; git add -A; git commit --all --verbose --message=\"$@\"; git push --verbose"
[apply]      ignorewhitespace = change
[blame]      coloring = highlightrecent
             date = format-local:%A %d %B %Y, %H%M %Z
[color]      advice = always
             branch = always
             diff = always
             grep = always
             interactive = auto
             push = always
             remote = always
             showbranch = always
             status = always
             transport = always
             ui = always
             autocrlf = input
[column]     ui = auto,dense
             status = plain
[commit]     verbose = true
[credential] helper = store
[core]       autocrlf = false
             editor = nano
             eol = lf
             sharedrepository = umask
             pager = less
             untrackedcache = keep
             whitespace = trailing-space,space-before-tab,cr-at-eol,tabwidth=2
[diff]       colormoved = zebra
             colormovedws = ignore-space-at-eol
[grep]       linenumber = true
             patterntype = extended
[gui]        spellingdictionary = none
[help]       autocorrect = 1
             browser = w3m
             format = man
             htmlpath  = https://git-scm.com/docs
[http]       emptyauth = true
             sslversion = tlsv1.2
             sslverify = true
[log]        abbrevcommit = false
             date = format-local:%A %d %B %Y, %H%M %Z
             decorate = full
[merge]      verifysignatures = false
             renormalize = true
[push]       default = simple
             gpgsign = false
[rebase]     autostash = true
[status]     relativepaths = false
             renames = true
             showstash = true
             showuntrackedfiles = all
[user]       useconfigonly = true
             name = Hunter Richardson
[web]        browser = w3m
